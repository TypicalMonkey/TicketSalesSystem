<Window x:Class="TicketSalesSystem.Views.TicketSaleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ticket Sale" Height="400" Width="600">
    <Grid>
        <DataGrid x:Name="dgRoutes" AutoGenerateColumns="False" Margin="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Departure Time" Binding="{Binding DepartureTime}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 20 0 0">
            <TextBlock Text="Select Route:" Margin="0 0 0 10"/>
            <ComboBox x:Name="cbRoutes" Width="300" Margin="0 0 0 10"/>
            <TextBlock Text="Price:" Margin="0 0 0 5"/>
            <TextBox x:Name="txtPrice" Width="100" Margin="0 0 0 10"/>
            <Button Content="Sell Ticket" Width="100" />
        </StackPanel>
    </Grid>
</Window>
